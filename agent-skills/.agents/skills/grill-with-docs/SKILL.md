---
name: grill-with-docs
description: A relentless interview to sharpen a plan or design, which also creates docs (ADR's and glossary) as we go.
disable-model-invocation: true
---

Load and follow the installed `grilling` and `domain-modeling` skills together. On a harness without a `Skill` tool, read both installed `SKILL.md` files directly before continuing.
